# SwarmVault Open Source

This workspace contains the open source runtime:

- `engine`: ingest, compile, query, lint, and provider abstractions
- `cli`: the `swarmvault` command
- `viewer`: the local graph explorer

## Global Install

```bash
npm install -g @swarmvaultai/cli
```
